
// This file contains API configuration
// In a production environment, these would be environment variables
// Note: For a real application, do not store API keys directly in the codebase

export const API_KEYS = {
  // Gemini API key
  GEMINI_API_KEY: "AIzaSyCeLn2sUjEmAi0BtqYmBDCCnCh_eQzAXII",
  
  // We no longer need Google Places API key as we're using OpenStreetMap/Overpass API
  // which doesn't require API keys
};
